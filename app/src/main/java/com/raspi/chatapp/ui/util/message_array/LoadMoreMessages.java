package com.raspi.chatapp.ui.util.message_array;

/**
 * an instance of this signals that there should be a field containing the
 * loadMoreMessagesButton. Nope, it does not contain anything. I like
 * container for nothing.
 */
public class LoadMoreMessages extends MessageArrayContent{
  /**
   * it creates something and nothing at the same time
   */
  public LoadMoreMessages(){
  }
}
