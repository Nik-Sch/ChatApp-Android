<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                tools:context="com.raspi.chatapp.ui.password.PinFragment">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/pin"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:padding="40dp"
        android:adjustViewBounds="true"
        android:src="@drawable/launcher_icon"/>

    <TextView
        android:id="@+id/password_invalid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/pin"
        android:text="@string/invalid_pwd"
        android:layout_centerHorizontal="true"
        android:padding="5dp"
        android:textSize="17sp"
        android:textColor="@color/incorrect"
        android:visibility="gone"/>

    <EditText
        android:id="@+id/pin"
        android:layout_width="95sp"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:inputType="numberPassword"
        android:maxEms="4"
        android:textSize="35sp"
        android:background="@color/new_message_bg"/>
</RelativeLayout>
